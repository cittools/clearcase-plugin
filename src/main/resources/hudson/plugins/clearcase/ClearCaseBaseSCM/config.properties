##############################
# ClearCase Base SCM messages #
##############################

useDynamicView=Work with dynamic views

useUpdate=Re-use existing view
useUpdateComment=If checked, Hudson will try to use an existing view. If the view doesn't exist, \
it will be created.

viewName=View name

configSpec=Config spec

loadRules=Load rules

clearcaseConfig=Clearcase configuration

branch=Branch(es)

If the view already exists and this option unchecked, the view will be deleted at the begining of each build.

excludedRegions=Excluded regions

mkviewOptionalParam=Additional <tt>mkview</tt> arguments

multiSitePollBuffer=Multi-site poll buffer

filteringOutDestroySubBranchEvent=Ignore "destroy sub branch" events
filteringOutDestroySubBranchEventComment=If checked, Hudson will filter out \
<i>Destroy sub-branch "BRANCHNAME" of branch</i> events when polling and retrieving \
change logs from a ClearCase repository.

viewDrive=Dynamic views root

doNotUpdateConfigSpec=Do not reset config spec
doNotUpdateConfigSpecComment=If checked, Hudson will not use <tt>cleartool setcs</tt> \
even if the specifed config spec is different from the dynamic view's current config spec.

customWorkspace=SCM Custom workspace